import React from "react";
import PropTypes from "prop-types";

const QueryCard = (cardData) => {

    
  return <div>card data</div>;
};

QueryCard.propTypes = {
  cardData: PropTypes.object,
};

export default QueryCard;
