import PropTypes from 'prop-types';

const AllQueries = props => {


    
    return (
        <div>
all queries                
        </div>
    );
};

AllQueries.propTypes = {
    
};

export default AllQueries;